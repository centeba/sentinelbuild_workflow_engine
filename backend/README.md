# mit-stack backend

Backend service for the Mit Stack automation packs (workflows, rules, AI agents,
data domains). Part of the SentinelBuild platform.

The pyproject references this file via `readme = "README.md"`; it must exist for
`pip install`/editable metadata builds (e.g. CI) to succeed.
